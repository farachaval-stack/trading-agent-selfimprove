# Self-Improving Trading Agent

Local scaffold for a paper-mode, self-improving trading worker plus phase prompts for onboarding and deployment.

Start with `prompts/00_master_context.md`, then run the phase prompts in order.

All project files live in this folder. The Railway container uses `/app`, with `state/` mounted as persistent storage.
